{
	"statusLine": {
		"type": "command",
		"command": "bash ~/.dotFiles/configs/claude/statusline-command.sh"
	},
	"permissions": {
		"deny": [
			"Bash(curl *)",
			"Bash(wget *)",
			"Bash(sudo:*)",
			"Bash(su:*)",
			"Bash(rm -rf *)",
			"Bash(git push --force *)",
			"Read(./.env)",
			"Read(./.env.*)",
			"Read(./secrets/**)",
			"Edit(*.key)",
			"Edit(*.pem)"
		],
		"defaultMode": "plan"
	},
	"env": {
		"CLAUDE_CODE_DISABLE_AUTOUPDATER": "1"
	}
}
